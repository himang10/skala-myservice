# 04.mariadb

이 디렉토리는 **MariaDB + Debezium CDC 실습용 설정 파일**을 모아둔 영역입니다.

## 이 디렉토리 기준 구성

- `mariadb-custom.cnf`: Binlog/복제 관련 설정 포함 MariaDB 커스텀 설정
- `setup_debezium_user.sql`: CDC용 사용자/권한 초기화 SQL

## 한 줄 요약

MCP 서버와 CDC 실습에서 사용할 MariaDB 실행 환경을 맞추기 위한 설정 파일 모음입니다.
