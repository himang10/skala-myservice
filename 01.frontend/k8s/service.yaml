apiVersion: v1
kind: Service
metadata:
  name: sk000-posts-get
  namespace: skala-practice
spec:
  selector:
    app: sk000-posts-get
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 80
  type: ClusterIP


